version: "3"

services:
  langchat-ui:
    image: registry.cn-beijing.aliyuncs.com/langchat/langchat-ui
    restart: always
    ports:
      - 3010:80
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    networks:
      - langchat-net

networks:
  langchat-net:
    driver: bridge
